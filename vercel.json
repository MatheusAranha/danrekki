{
  "buildCommand": "echo skip",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install --include=dev && ./node_modules/.bin/vite build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
